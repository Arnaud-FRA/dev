let nombre = parseInt(window.prompt("Entrez une valeur numérique"))
let resultat = 0

    for (i =1;i<=10;i++)
        {
        resultat = nombre * i
        console.log(nombre +"*" + i + "=" + resultat)
        }
        